<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://kelegele.com</id>
    <title>可了个乐</title>
    <updated>2020-05-06T07:18:59.240Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://kelegele.com"/>
    <link rel="self" href="https://kelegele.com/atom.xml"/>
    <subtitle>关于学习🐳与心得💕</subtitle>
    <logo>https://kelegele.com/images/avatar.png</logo>
    <icon>https://kelegele.com/favicon.ico</icon>
    <rights>All rights reserved 2020, 可了个乐</rights>
    <entry>
        <title type="html"><![CDATA[使用 Gitbook 搭建 book page]]></title>
        <id>https://kelegele.com/post/gitbook_start/</id>
        <link href="https://kelegele.com/post/gitbook_start/">
        </link>
        <updated>2019-08-16T07:36:02.000Z</updated>
        <summary type="html"><![CDATA[<p>一个简洁的gitbook搭建教程。</p>
]]></summary>
        <content type="html"><![CDATA[<p>一个简洁的gitbook搭建教程。</p>
<!-- more -->
<h1 id="1-gitbook是什么">1 Gitbook是什么</h1>
<p>GitBook是一个命令行工具（通过使用Node.js），用于使用GitHub / Git通过Markdown（或AsciiDoc）构建文档、页面以及书籍。本文将以构建一本书籍为例进行说明。</p>
<h1 id="2-开始gitbook">2 开始Gitbook</h1>
<blockquote>
<p>安装前请先安装Node.js和npm工具</p>
</blockquote>
<h2 id="21-通过npm安装">2.1 通过npm安装</h2>
<p>安装Gitbook最方便的的方法就是通过npm一条指令进行安装：</p>
<pre><code class="language-bash">$ npm install gitbook-cli -g
</code></pre>
<p><code>gitbook-cli</code>是一个在系统上安装和使用GitBook的npm包程序，自动安装所需版本的GitBook来构建书籍。<br>
可通过<code>gitbook-cli</code>来切换<code>gitbook</code>的版本：</p>
<pre><code class="language-bash">$ gitbook fetch beta
</code></pre>
<p>使用<code>gitbook ls-remote</code>可罗列出所有可用版本。<br>
执行后截图：<br>
<img src="https://kelegele.com/post-images/1565967559383.PNG" alt="image" loading="lazy"><br>
由于npm网络太慢，这里我采用了cnpm的淘宝镜像进行安装。<br>
安装完成后测试指令：</p>
<pre><code class="language-bsah">$ gitbook -help
</code></pre>
<p>成功截图：<br>
<img src="https://kelegele.com/post-images/1565967609172.PNG" alt="image" loading="lazy"></p>
<h2 id="22-创建书籍">2.2 创建书籍</h2>
<p>在创建之前新建我们想要保存这些构建文件的根目录，在该目录下打开命令行，使用初始化命令构建书本：</p>
<pre><code class="language-bsah">$ gitbook init
</code></pre>
<p>如果我们不想在本目录创建书籍，可以使用以下命令指定目录：</p>
<pre><code class="language-bash">$ gitbook init ./directory
</code></pre>
<p>创建完成后会生成两个文件：<br>
<img src="https://kelegele.com/post-images/1565967618987.PNG" alt="image" loading="lazy"><br>
README.md 和 SUMMARY.md 是两个必须文件，README.md 是对书籍的简单介绍，SUMMARY.md 是书籍的目录结构。</p>
<h2 id="23-预览-构建静态页面">2.3 预览、构建静态页面</h2>
<p>创建完成后可通过以下指令预览：</p>
<pre><code class="language-bash">$ gitbook serve
</code></pre>
<p><img src="https://kelegele.com/post-images/1565967633001.PNG" alt="image" loading="lazy"><br>
在浏览器打开<code>http://localhost:4000/</code>即可预览：<br>
<img src="https://kelegele.com/post-images/1565967641876.PNG" alt="image" loading="lazy"></p>
<p>书籍制作完成后也可以直接构建成静态页面：</p>
<pre><code class="language-bash">$ gitbook build ./repository --output=./outputFolder
</code></pre>
<p>构建完成后将会在目录生成相应的静态页面文件夹。<br>
<img src="https://kelegele.com/post-images/1565967654017.PNG" alt="image" loading="lazy"><br>
Gitbook 创建了书籍目录结构后，我们就可以向其中添加真正的内容了，文件的编写使用 markdown 语法，在内容修改过程中，每一次保存文件，<code>gitbook serve</code> 都会自动重新编译，所以可以持续通过浏览器来查看最新的书籍效果！</p>
<p>命令 build 和 serve 的参数为:</p>
<pre><code> -o, --output &lt;directory&gt;  #输出文件件, 默认为 ./_book
 -f, --format &lt;name&gt;       #产生的书籍的类型, 默认为静态站点, 可用的格式为: site, page, ebook, json
 --config &lt;config file&gt;    #配置文件, 默认为 book.js 或 book.json
</code></pre>
<h2 id="24-gitbook-editor编辑器">2.4 GitBook Editor编辑器</h2>
<p>可以下载 gitbook 编辑器，做到所见即所得的编辑，如下图所示：<br>
<img src="https://kelegele.com/post-images/1565967667144.png" alt="image" loading="lazy"><br>
官方网址：<a href="https://legacy.gitbook.com/editor">https://legacy.gitbook.com/editor</a></p>
<h1 id="3-配置">3 配置</h1>
<p>Gitbook允许使用灵活的配置自定义书籍，配置选项在<code>book.json</code>或<code>book.js</code>文件中填写，不熟悉JSON语法可以使用JSONlint等工具验证语法。</p>
<h2 id="31-常规设置">3.1 常规设置</h2>
<ul>
<li><code>root</code>	包含所有书籍文件的根文件夹的路径，除外 book.json</li>
<li><code>structure</code>	指定自述文件，摘要，词汇表等的路径。见3.2结构段落。</li>
<li><code>title</code>	书籍的标题，默认值是从README中提取的。</li>
<li><code>description</code>	您的书的描述，默认值是从README中提取的。</li>
<li><code>author</code>	作者姓名。</li>
<li><code>isbn</code>	这本书的ISBN。</li>
<li><code>language</code>	本书语言的ISO语言代码，默认值为en。</li>
<li><code>direction</code>	文字的方向。可以是rtl或者ltr，默认值取决于值language。</li>
<li><code>gitbook</code>	应该使用的GitBook版本。使用<a href="http://semver.org/">SemVer</a>规范并接受类似的条件&quot;&gt;= 3.0.0&quot;。</li>
</ul>
<h2 id="32-结构段落structure">3.2 结构段落structure</h2>
<ul>
<li>structure.readme	自述文件名（默认为README.md）</li>
<li>structure.summary	摘要文件名（默认为SUMMARY.md）</li>
<li>structure.glossary	词汇表文件名（默认为GLOSSARY.md）</li>
<li>structure.languages	语言文件名（默认为LANGS.md）</li>
</ul>
<h2 id="33-插件plugins">3.3 插件Plugins</h2>
<p>插件及其配置在<code>book.json</code>中指定。<br>
从版本3.0.0开始，GitBook可以使用主题。</p>
<ul>
<li>plugins	要加载的插件列表</li>
<li>pluginsConfig	插件配置</li>
</ul>
<h2 id="34-pdf选项">3.4 PDF选项</h2>
<p>PDF输出可以使用以下选项中的一组选项进行自定义<code>book.json</code>：</p>
<ul>
<li>pdf.pageNumbers	将页码添加到每个页面的底部（默认为true）</li>
<li>pdf.fontSize	基本字体大小（默认为12）</li>
<li>pdf.fontFamily	基本字体系列（默认为Arial）</li>
<li>pdf.paperSize	纸张尺寸，选项是'a0', 'a1', 'a2', 'a3', 'a4', 'a5', 'a6', 'b0', 'b1', 'b2', 'b3', 'b4', 'b5', 'b6', 'legal', 'letter'（默认是a4）</li>
<li>pdf.margin.top	上边距（默认为56）</li>
<li>pdf.margin.bottom	底部保证金（默认为56）</li>
<li>pdf.margin.right	右边距（默认为62）</li>
<li>pdf.margin.left	左边距（默认为62）</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[常用正则表达式]]></title>
        <id>https://kelegele.com/post/common_RegExr/</id>
        <link href="https://kelegele.com/post/common_RegExr/">
        </link>
        <updated>2019-08-05T07:05:48.000Z</updated>
        <summary type="html"><![CDATA[<p>一些常用的正则表达式导航。</p>
]]></summary>
        <content type="html"><![CDATA[<p>一些常用的正则表达式导航。</p>
<!-- more -->
<p>[1].<a href="https://github.com/cdoco/common-regex">菜鸟教程</a><br>
[2].<a href="https://c.runoob.com/front-end/854">GitHub:cdoco</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[正则表达式（Regular Expression）]]></title>
        <id>https://kelegele.com/post/Regular_Expressions/</id>
        <link href="https://kelegele.com/post/Regular_Expressions/">
        </link>
        <updated>2019-08-05T03:06:07.000Z</updated>
        <summary type="html"><![CDATA[<p>正则表达式无论在前端还是后台的开发过程中，是程序员必不可少的技能。本文整理了网上一些正则表达式的笔记仅供参考。</p>
]]></summary>
        <content type="html"><![CDATA[<p>正则表达式无论在前端还是后台的开发过程中，是程序员必不可少的技能。本文整理了网上一些正则表达式的笔记仅供参考。</p>
<!-- more -->
<style>
table{
	width:100%;
}
table th {
    white-space: nowrap; /*表头内容强制在一行显示*/
}
table th {
    font-weight: bold; /*加粗*/
    text-align: center !important; 
	/*内容居中，加上 !important 避免被 Markdown 样式覆盖*/
   background: rgba(231,233,234,0.8); 
}
table td {
	word-wrap:break-word;
	}
</style>
<blockquote>
<p>正则表达式（英语：Regular Expression，在代码中常简写为regex、regexp或RE），又称正规表示式、正规表示法、规则运算式、常规表示法，是计算机科学的一个概念。正则表达式使用单个字符串来描述、匹配一系列符合某个句法规则的字符串。在很多文本编辑器里，正则表达式通常被用来检索、替换那些符合某个模式的文本。<a href="https://zh.wikipedia.org/zh-hans/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F">wiki</a><br>
本文对于正则表达式的发展历史和标准概要不做过多赘述，直接进行笔记式说明正则表达式，更像是集成了网上的一些热门教程。</p>
</blockquote>
<h1 id="学习-检验工具">学习、检验工具</h1>
<p>正则表达式或说正则匹配在初学者当中并不能马上看出对错，往往需要借助匹配工具，当然，如果你不嫌麻烦，你也可以在敲代码的同时不停地debug你的代码来检验正则匹配。<br>
工具很多，我在这推荐<a href="https://regexr.com">RegExr: Learn, Build, &amp; Test RegEx</a>这个在线正则匹配工具。<br>
<img src="https://kelegele.com/post-images/1564976524676.png" alt="image" loading="lazy"></p>
<h1 id="语法">语法</h1>
<p>构造正则表达式的方法和创建数学表达式的方法一样。也就是用多种元字符与运算符可以将小的表达式结合在一起来创建更大的表达式。正则表达式的组件可以是单个的字符、字符集合、字符范围、字符间的选择或者所有这些组件的任意组合。</p>
<p>正则表达式是由普通字符（例如字符 a 到 z）以及特殊字符（称为&quot;元字符&quot;）组成的文字模式。模式描述在搜索文本时要匹配的一个或多个字符串。正则表达式作为一个模板，将某个字符模式与所搜索的字符串进行匹配。</p>
<h2 id="普通字符">普通字符</h2>
<p>普通字符包括没有显式指定为元字符的所有可打印和不可打印字符。包括所有大写和小写字母、所有数字、所有标点符号和一些其他符号。</p>
<h2 id="非打印字符">非打印字符</h2>
<p>非打印字符也可以是正则表达式的组成部分。</p>
<table>
<thead>
<tr>
<th>字 符</th>
<th>描 述</th>
</tr>
</thead>
<tbody>
<tr>
<td>\cx</td>
<td>匹配由x指明的控制字符。例如， \cM 匹配一个 Control-M 或回车符。x 的值必须为 A-Z 或 a-z 之一。否则，将 c 视为一个原义的 'c' 字符。</td>
</tr>
<tr>
<td>\f</td>
<td>匹配一个换页符。等价于 \x0c 和 \cL。</td>
</tr>
<tr>
<td>\n</td>
<td>匹配一个换行符。等价于 \x0a 和 \cJ。</td>
</tr>
<tr>
<td>\r</td>
<td>匹配一个回车符。等价于 \x0d 和 \cM。</td>
</tr>
<tr>
<td>\s</td>
<td>匹配任何空白字符，包括空格、制表符、换页符等等。等价于 [ \f\n\r\t\v]。注意 Unicode 正则表达式会匹配全角空格符。</td>
</tr>
<tr>
<td>\S</td>
<td>匹配任何非空白字符。等价于 [^ \f\n\r\t\v]。</td>
</tr>
<tr>
<td>\t</td>
<td>匹配一个制表符。等价于 \x09 和 \cI。</td>
</tr>
<tr>
<td>\v</td>
<td>匹配一个垂直制表符。等价于 \x0b 和 \cK。</td>
</tr>
</tbody>
</table>
<h2 id="特殊字符">特殊字符</h2>
<p>所谓特殊字符，就是一些有特殊含义的字符。<br>
许多元字符要求在试图匹配它们时特别对待。若要匹配这些特殊字符，必须首先使字符&quot;转义&quot;，即，将反斜杠字符<code>\</code>放在它们前面。</p>
<table>
<thead>
<tr>
<th>特别字符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>$</td>
<td>匹配输入字符串的结尾位置。如果设置了 RegExp 对象的 Multiline 属性，则 $ 也匹配 '\n' 或 '\r'。</td>
</tr>
<tr>
<td>( )</td>
<td>标记一个子表达式的开始和结束位置。子表达式可以获取供以后使用。</td>
</tr>
<tr>
<td>*</td>
<td>匹配前面的子表达式零次或多次。</td>
</tr>
<tr>
<td>+</td>
<td>匹配前面的子表达式一次或多次。</td>
</tr>
<tr>
<td><strong>.</strong></td>
<td>匹配除换行符 \n 之外的任何单字符。</td>
</tr>
<tr>
<td><strong>[</strong></td>
<td>标记一个中括号表达式的开始。</td>
</tr>
<tr>
<td>?</td>
<td>匹配前面的子表达式零次或一次，或指明一个非贪婪限定符。</td>
</tr>
<tr>
<td>^</td>
<td>匹配输入字符串的开始位置，除非在方括号表达式中使用，此时它表示不接受该字符集合。</td>
</tr>
<tr>
<td>{</td>
<td>标记限定符表达式的开始。</td>
</tr>
<tr>
<td><strong>|</strong></td>
<td>指明两项之间的一个选择。</td>
</tr>
<tr>
<td>\(转义字符)</td>
<td>将下一个字符标记为或特殊字符、或原义字符、或向后引用、或八进制转义符。例如， 'n' 匹配字符 'n'。'\n' 匹配换行符。序列 '\\' 匹配 &quot;\&quot;，而 '\(' 则匹配 &quot;(&quot;。</td>
</tr>
</tbody>
</table>
<h2 id="限定符">限定符</h2>
<p>限定符用来指定正则表达式的一个给定组件必须要出现多少次才能满足匹配。有 * 或 + 或 ? 或 {n} 或 {n,} 或 {n,m} 共6种。</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>*</td>
<td>匹配前面的子表达式零次或多次。例如，zo* 能匹配 &quot;z&quot; 以及 &quot;zoo&quot;。* 等价于{0,}。</td>
</tr>
<tr>
<td>+</td>
<td>匹配前面的子表达式一次或多次。例如，'zo+' 能匹配 &quot;zo&quot; 以及 &quot;zoo&quot;，但不能匹配 &quot;z&quot;。+ 等价于 {1,}。</td>
</tr>
<tr>
<td>?</td>
<td>匹配前面的子表达式零次或一次。例如，&quot;do(es)?&quot; 可以匹配 &quot;do&quot; 、 &quot;does&quot; 中的 &quot;does&quot; 、 &quot;doxy&quot; 中的 &quot;do&quot; 。? 等价于 {0,1}。</td>
</tr>
<tr>
<td>{n}	n</td>
<td>是一个非负整数。匹配确定的 n 次。例如，'o{2}' 不能匹配 &quot;Bob&quot; 中的 'o'，但是能匹配 &quot;food&quot; 中的两个 o。</td>
</tr>
<tr>
<td>{n,}</td>
<td>n 是一个非负整数。至少匹配n 次。例如，'o{2,}' 不能匹配 &quot;Bob&quot; 中的 'o'，但能匹配 &quot;foooood&quot; 中的所有 o。'o{1,}' 等价于 'o+'。'o{0,}' 则等价于 'o*'。</td>
</tr>
<tr>
<td>{n,m}</td>
<td>m 和 n 均为非负整数，其中n &lt;= m。最少匹配 n 次且最多匹配 m 次。例如，&quot;o{1,3}&quot; 将匹配 &quot;fooooood&quot; 中的前三个 o。'o{0,1}' 等价于 'o?'。请注意在逗号和两个数之间不能有空格。</td>
</tr>
</tbody>
</table>
<h2 id="定位符">定位符</h2>
<p>定位符使您能够将正则表达式固定到行首或行尾。它们还使您能够创建这样的正则表达式，这些正则表达式出现在一个单词内、在一个单词的开头或者一个单词的结尾。<br>
定位符用来描述字符串或单词的边。</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>^</td>
<td>匹配输入字符串开始的位置。如果设置了 RegExp 对象的 Multiline 属性，^ 还会与 \n 或 \r 之后的位置匹配。</td>
</tr>
<tr>
<td>$</td>
<td>匹配输入字符串结尾的位置。如果设置了 RegExp 对象的 Multiline 属性，$ 还会与 \n 或 \r 之前的位置匹配。</td>
</tr>
<tr>
<td>\b</td>
<td>匹配一个单词边界，即字与空格间的位置。</td>
</tr>
<tr>
<td>\B</td>
<td>非单词边界匹配。</td>
</tr>
</tbody>
</table>
<p>注意：不能将限定符与定位符一起使用。由于在紧靠换行或者单词边界的前面或后面不能有一个以上位置，因此不允许诸如 ^* 之类的表达式。</p>
<h2 id="选择">选择</h2>
<p>用圆括号将所有选择项括起来，相邻的选择项之间用|分隔。但用圆括号会有一个副作用，使相关的匹配会被缓存，此时可用?:放在第一个选项前来消除这种副作用。</p>
<p>其中 ?: 是非捕获元之一，还有两个非捕获元是 ?= 和 ?!，这两个还有更多的含义，前者为正向预查，在任何开始匹配圆括号内的正则表达式模式的位置来匹配搜索字符串，后者为负向预查，在任何开始不匹配该正则表达式模式的位置来匹配搜索字符串。</p>
<h2 id="反向引用">反向引用</h2>
<p>对一个正则表达式模式或部分模式两边添加圆括号将导致相关匹配存储到一个临时缓冲区中，所捕获的每个子匹配都按照在正则表达式模式中从左到右出现的顺序存储。缓冲区编号从 1 开始，最多可存储 99 个捕获的子表达式。每个缓冲区都可以使用 \n 访问，其中 n 为一个标识特定缓冲区的一位或两位十进制数。</p>
<h1 id="元字符">元字符</h1>
<p>下表包含了元字符的完整列表以及它们在正则表达式上下文中的行为：</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>\</td>
<td>将下一个字符标记为一个特殊字符、或一个原义字符、或一个 向后引用、或一个八进制转义符。例如，'n' 匹配字符 &quot;n&quot;。'\n' 匹配一个换行符。序列 '\' 匹配 &quot;&quot; 而 &quot;(&quot; 则匹配 &quot;(&quot;。</td>
</tr>
<tr>
<td>^</td>
<td>匹配输入字符串的开始位置。如果设置了 RegExp 对象的 Multiline 属性，^ 也匹配 '\n' 或 '\r' 之后的位置。</td>
</tr>
<tr>
<td>$</td>
<td>匹配输入字符串的结束位置。如果设置了RegExp 对象的 Multiline 属性，$ 也匹配 '\n' 或 '\r' 之前的位置。</td>
</tr>
<tr>
<td>*</td>
<td>匹配前面的子表达式零次或多次。例如，zo* 能匹配 &quot;z&quot; 以及 &quot;zoo&quot;。* 等价于{0,}。</td>
</tr>
<tr>
<td>+</td>
<td>匹配前面的子表达式一次或多次。例如，'zo+' 能匹配 &quot;zo&quot; 以及 &quot;zoo&quot;，但不能匹配 &quot;z&quot;。+ 等价于 {1,}。</td>
</tr>
<tr>
<td>?</td>
<td>匹配前面的子表达式零次或一次。例如，&quot;do(es)?&quot; 可以匹配 &quot;do&quot; 或 &quot;does&quot; 。? 等价于 {0,1}。</td>
</tr>
<tr>
<td>{n}</td>
<td>n 是一个非负整数。匹配确定的 n 次。例如，'o{2}' 不能匹配 &quot;Bob&quot; 中的 'o'，但是能匹配 &quot;food&quot; 中的两个 o。</td>
</tr>
<tr>
<td>{n,}</td>
<td>n 是一个非负整数。至少匹配n 次。例如，'o{2,}' 不能匹配 &quot;Bob&quot; 中的 'o'，但能匹配 &quot;foooood&quot; 中的所有 o。'o{1,}' 等价于 'o+'。'o{0,}' 则等价于 'o*'。</td>
</tr>
<tr>
<td>{n,m}</td>
<td>m 和 n 均为非负整数，其中n &lt;= m。最少匹配 n 次且最多匹配 m 次。例如，&quot;o{1,3}&quot; 将匹配 &quot;fooooood&quot; 中的前三个 o。'o{0,1}' 等价于 'o?'。请注意在逗号和两个数之间不能有空格。</td>
</tr>
<tr>
<td>?</td>
<td>当该字符紧跟在任何一个其他限制符 (*, +, ?, {n}, {n,}, {n,m}) 后面时，匹配模式是非贪婪的。非贪婪模式尽可能少的匹配所搜索的字符串，而默认的贪婪模式则尽可能多的匹配所搜索的字符串。例如，对于字符串 &quot;oooo&quot;，'o+?' 将匹配单个 &quot;o&quot;，而 'o+' 将匹配所有 'o'。</td>
</tr>
<tr>
<td>.</td>
<td>匹配除换行符（\n、\r）之外的任何单个字符。要匹配包括 '\n' 在内的任何字符，请使用像&quot;(.</td>
</tr>
<tr>
<td>(pattern)</td>
<td>匹配 pattern 并获取这一匹配。所获取的匹配可以从产生的 Matches 集合得到，在VBScript 中使用 SubMatches 集合，在JScript 中则使用 $0…$9 属性。要匹配圆括号字符，请使用 '(' 或 ')'。</td>
</tr>
<tr>
<td>(?:pattern)</td>
<td>匹配 pattern 但不获取匹配结果，也就是说这是一个非获取匹配，不进行存储供以后使用。这在使用 &quot;或&quot; 字符 (</td>
</tr>
<tr>
<td>(?=pattern)</td>
<td>正向肯定预查（look ahead positive assert），在任何匹配pattern的字符串开始处匹配查找字符串。这是一个非获取匹配，也就是说，该匹配不需要获取供以后使用。例如，&quot;Windows(?=95</td>
</tr>
<tr>
<td>(?!pattern)</td>
<td>正向否定预查(negative assert)，在任何不匹配pattern的字符串开始处匹配查找字符串。这是一个非获取匹配，也就是说，该匹配不需要获取供以后使用。例如&quot;Windows(?!95</td>
</tr>
<tr>
<td>(?&lt;=pattern)</td>
<td>反向(look behind)肯定预查，与正向肯定预查类似，只是方向相反。例如，&quot;(?&lt;=95</td>
</tr>
<tr>
<td>(?&lt;!pattern)</td>
<td>反向否定预查，与正向否定预查类似，只是方向相反。例如&quot;(?&lt;!95</td>
</tr>
<tr>
<td>x|y</td>
<td>匹配 x 或 y。例如，'z</td>
</tr>
<tr>
<td>[xyz]</td>
<td>字符集合。匹配所包含的任意一个字符。例如， '[abc]' 可以匹配 &quot;plain&quot; 中的 'a'。</td>
</tr>
<tr>
<td>[^xyz]</td>
<td>负值字符集合。匹配未包含的任意字符。例如， '[^abc]' 可以匹配 &quot;plain&quot; 中的'p'、'l'、'i'、'n'。</td>
</tr>
<tr>
<td>[a-z]</td>
<td>字符范围。匹配指定范围内的任意字符。例如，'[a-z]' 可以匹配 'a' 到 'z' 范围内的任意小写字母字符。</td>
</tr>
<tr>
<td>[^a-z]</td>
<td>负值字符范围。匹配任何不在指定范围内的任意字符。例如，'[^a-z]' 可以匹配任何不在 'a' 到 'z' 范围内的任意字符。</td>
</tr>
<tr>
<td>\b</td>
<td>匹配一个单词边界，也就是指单词和空格间的位置。例如， 'er\b' 可以匹配&quot;never&quot; 中的 'er'，但不能匹配 &quot;verb&quot; 中的 'er'。</td>
</tr>
<tr>
<td>\B</td>
<td>匹配非单词边界。'er\B' 能匹配 &quot;verb&quot; 中的 'er'，但不能匹配 &quot;never&quot; 中的 'er'。</td>
</tr>
<tr>
<td>\cx</td>
<td>匹配由 x 指明的控制字符。例如， \cM 匹配一个 Control-M 或回车符。x 的值必须为 A-Z 或 a-z 之一。否则，将 c 视为一个原义的 'c' 字符。</td>
</tr>
<tr>
<td>\d</td>
<td>匹配一个数字字符。等价于 [0-9]。</td>
</tr>
<tr>
<td>\D</td>
<td>匹配一个非数字字符。等价于 [^0-9]。</td>
</tr>
<tr>
<td>\f</td>
<td>匹配一个换页符。等价于 \x0c 和 \cL。</td>
</tr>
<tr>
<td>\n</td>
<td>匹配一个换行符。等价于 \x0a 和 \cJ。</td>
</tr>
<tr>
<td>\r</td>
<td>匹配一个回车符。等价于 \x0d 和 \cM。</td>
</tr>
<tr>
<td>\s</td>
<td>匹配任何空白字符，包括空格、制表符、换页符等等。等价于 [ \f\n\r\t\v]。</td>
</tr>
<tr>
<td>\S</td>
<td>匹配任何非空白字符。等价于 [^ \f\n\r\t\v]。</td>
</tr>
<tr>
<td>\t</td>
<td>匹配一个制表符。等价于 \x09 和 \cI。</td>
</tr>
<tr>
<td>\v</td>
<td>匹配一个垂直制表符。等价于 \x0b 和 \cK。</td>
</tr>
<tr>
<td>\w</td>
<td>匹配字母、数字、下划线。等价于'[A-Za-z0-9_]'。</td>
</tr>
<tr>
<td>\W</td>
<td>匹配非字母、数字、下划线。等价于 '[^A-Za-z0-9_]'。</td>
</tr>
<tr>
<td>\xn</td>
<td>匹配 n，其中 n 为十六进制转义值。十六进制转义值必须为确定的两个数字长。例如，'\x41' 匹配 &quot;A&quot;。'\x041' 则等价于 '\x04' &amp; &quot;1&quot;。正则表达式中可以使用 ASCII 编码。</td>
</tr>
<tr>
<td>\num</td>
<td>匹配 num，其中 num 是一个正整数。对所获取的匹配的引用。例如，'(.)\1' 匹配两个连续的相同字符。</td>
</tr>
<tr>
<td>\n</td>
<td>标识一个八进制转义值或一个向后引用。如果 \n 之前至少 n 个获取的子表达式，则 n 为向后引用。否则，如果 n 为八进制数字 (0-7)，则 n 为一个八进制转义值。</td>
</tr>
<tr>
<td>\nm</td>
<td>标识一个八进制转义值或一个向后引用。如果 \nm 之前至少有 nm 个获得子表达式，则 nm 为向后引用。如果 \nm 之前至少有 n 个获取，则 n 为一个后跟文字 m 的向后引用。如果前面的条件都不满足，若 n 和 m 均为八进制数字 (0-7)，则 \nm 将匹配八进制转义值 nm。</td>
</tr>
<tr>
<td>\nml</td>
<td>如果 n 为八进制数字 (0-3)，且 m 和 l 均为八进制数字 (0-7)，则匹配八进制转义值 nml。</td>
</tr>
<tr>
<td>\un</td>
<td>匹配 n，其中 n 是一个用四个十六进制数字表示的 Unicode 字符。例如， \u00A9 匹配版权符号 (?)。</td>
</tr>
</tbody>
</table>
<h1 id="运算符优先级">运算符优先级</h1>
<p>正则表达式从左到右进行计算，并遵循优先级顺序，这与算术表达式非常类似。</p>
<p>相同优先级的从左到右进行运算，不同优先级的运算先高后低。下表从最高到最低说明了各种正则表达式运算符的优先级顺序：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>\</td>
<td>转义符</td>
</tr>
<tr>
<td>(), (?😃, (?=), []</td>
<td>圆括号和方括号</td>
</tr>
<tr>
<td>*, +, ?, {n}, {n,}, {n,m}</td>
<td>限定符</td>
</tr>
<tr>
<td>^, $, \任何元字符、任何字符</td>
<td>定位点和序列（即：位置和顺序）</td>
</tr>
<tr>
<td>\</td>
<td>替换，&quot;或&quot;操作 字符具有高于替换运算符的优先级，使得&quot;m</td>
</tr>
</tbody>
</table>
<h1 id="匹配规则">匹配规则</h1>
<h2 id="基本模式匹配">基本模式匹配</h2>
<p>模式，是正则表达式最基本的元素，它们是一组描述字符串特征的字符。模式可以很简单，由普通的字符串组成，也可以非常复杂，往往用特殊的字符表示一个范围内的字符、重复出现，或表示上下文。</p>
<h2 id="字符簇">字符簇</h2>
<ul>
<li>[a-z] //匹配所有的小写字母</li>
<li>[A-Z] //匹配所有的大写字母</li>
<li>[a-zA-Z] //匹配所有的字母</li>
<li>[0-9] //匹配所有的数字</li>
<li>[0-9.-] //匹配所有的数字，句号和减号</li>
<li>[ \f\r\t\n] //匹配所有的白字符</li>
</ul>
<p>前面曾经提到 <strong>^</strong> 表示字符串的开头，但它还有另外一个含义。当在一组方括号里使用 ^ 时，它表示 <strong>&quot;非&quot;</strong> 或 <strong>&quot;排除&quot;</strong> 的意思，常常用来剔除某个字符。</p>
<ul>
<li>[^a-z] //除了小写字母以外的所有字符</li>
<li>[^\/^] //除了()(/)(^)之外的所有字符</li>
<li>[^&quot;'] //除了双引号(&quot;)和单引号(')之外的所有字符</li>
</ul>
<h2 id="确定重复出现">确定重复出现</h2>
<p>到现在为止，你已经知道如何去匹配一个字母或数字，但更多的情况下，可能要匹配一个单词或一组数字。一个单词有若干个字母组成，一组数字有若干个单数组成。跟在字符或字符簇后面的花括号({})用来确定前面的内容的重复出现的次数。</p>
<table>
<thead>
<tr>
<th>字符簇</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>^[a-zA-Z_]$</td>
<td>所有的字母和下划线</td>
</tr>
<tr>
<td>^[[:alpha:]]{3}$</td>
<td>所有的3个字母的单词</td>
</tr>
<tr>
<td>^a$</td>
<td>字母a</td>
</tr>
<tr>
<td>^a{4}$</td>
<td>aaaa</td>
</tr>
<tr>
<td>^a{2,4}$</td>
<td>aa,aaa或aaaa</td>
</tr>
<tr>
<td>^a{1,3}$</td>
<td>a,aa或aaa</td>
</tr>
<tr>
<td>^a{2,}$</td>
<td>包含多于两个a的字符串</td>
</tr>
<tr>
<td>^a{2,}</td>
<td>如：aardvark和aaab，但apple不行</td>
</tr>
<tr>
<td>a{2,}</td>
<td>如：baad和aaa，但Nantucket不行</td>
</tr>
<tr>
<td>\t{2}</td>
<td>两个制表符</td>
</tr>
<tr>
<td>.{2}</td>
<td>所有的两个字符</td>
</tr>
</tbody>
</table>
<p>这些例子描述了花括号的三种不同的用法。一个数字 {x} 的意思是前面的字符或字符簇只出现x次 ；一个数字加逗号 {x,} 的意思是前面的内容出现x或更多的次数 ；两个数字用逗号分隔的数字 {x,y} 表示 前面的内容至少出现x次，但不超过y次。我们可以把模式扩展到更多的单词或数字：</p>
<ul>
<li>^[a-zA-Z0-9_]{1,}$      // 所有包含一个以上的字母、数字或下划线的字符串</li>
<li>^[1-9][0-9]{0,}$        // 所有的正整数</li>
<li>^-{0,1}[0-9]{1,}$      // 所有的整数</li>
<li>^[-]?[0-9]+.?[0-9]+$   // 所有的浮点数</li>
</ul>
<h1 id="参考整理">参考整理</h1>
<p>[1].<a href="https://www.runoob.com/regexp/regexp-tutorial.html">菜鸟教程正则表达式</a><br>
[2].<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Regular_Expressions">MDN正则表达式</a></p>
]]></content>
    </entry>
</feed>